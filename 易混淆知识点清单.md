# 数据结构易混淆知识点清单

## 1. 逻辑结构与存储结构

逻辑结构描述数据元素之间的关系，如线性、树形、图状；存储结构描述它们在内存中的组织方式，如顺序、链式、索引和散列。逻辑上相同的线性表可以采用顺序表或链表存储。

## 2. 数据结构与抽象数据类型

数据结构关注数据及其关系；ADT 同时规定数据对象、关系和允许的操作，但不限定具体实现。例如“栈”是 ADT，可以用数组或链表实现。

## 3. 时间复杂度与实际运行时间

时间复杂度描述输入规模增长时基本操作次数的数量级，不等于程序运行的秒数。同为 O(n) 的算法仍可能因常数、缓存和语言实现不同而有速度差异。

## 4. 最坏复杂度与平均复杂度

最坏复杂度给出所有同规模输入中的上界；平均复杂度需要输入概率分布。快速排序平均为 O(n log n)，最坏为 O(n²)。

## 5. 顺序表与链表

顺序表支持 O(1) 随机访问，但中间插入删除通常为 O(n)；链表按位访问为 O(n)，已知目标结点位置时修改链接可以是 O(1)。

## 6. 头指针与头结点

头指针是指向链表首部的指针；头结点是人为添加的辅助结点。即使没有头结点，链表仍必须有头指针。

## 7. 删除结点 O(1) 的前提

单链表删除某结点通常需要找到其前驱。只有已经掌握前驱指针，或允许复制后继结点数据等特殊条件时，才能视为 O(1)。

## 8. 栈与队列

栈是后进先出 LIFO，只在同一端操作；队列是先进先出 FIFO，通常一端入队、另一端出队。

## 9. 栈顶指针的两种定义

不同教材可能让 `top` 指向栈顶元素，也可能指向下一个可用位置。判断栈空、栈满公式必须结合题目定义，不能死记一个公式。

## 10. 循环队列判空与判满

若只使用 `front` 和 `rear`，`front == rear` 容易同时表示空和满。常见解决方案是牺牲一个存储单元、增加元素计数或增加标志位。

## 11. 递归与栈

递归调用由系统调用栈管理参数、局部变量和返回地址；“递归使用栈”不意味着所有使用栈的算法都是递归。

## 12. 满二叉树与完全二叉树

满二叉树每一层都达到最大结点数；完全二叉树只要求除最后一层外全满，最后一层结点从左向右连续排列。满二叉树一定是完全二叉树，反之不成立。

## 13. 二叉树与度为2的树

二叉树区分左右子树，即使只有一个孩子也要区分左或右；度为2的普通树通常不强调孩子次序，两者不是同一概念。

## 14. 结点的度与树的度

结点的度是该结点的孩子数；树的度是树中所有结点度数的最大值。

## 15. 前序、中序与后序遍历

三者区别在于访问根结点的时机：根左右、左根右、左右根。已知前序和后序通常不能唯一确定普通二叉树；加入中序后，在结点值互异时通常可以唯一确定。

## 16. 二叉排序树与堆

BST 要求左子树键值小于根、右子树大于根，便于查找；大根堆只保证父结点不小于孩子，不能通过中序遍历得到有序序列。

## 17. AVL树与完全二叉树

AVL 只要求每个结点左右子树高度差不超过1，不保证结点按完全二叉树方式排列；完全二叉树强调结点位置，不保证关键字有序。

## 18. B树与B+树

B树的内部结点和叶结点都可保存记录；B+树的实际记录通常只保存在叶结点，内部结点作为索引，叶结点还按顺序连接，更适合数据库范围查询。

## 19. 树的高度与深度

深度通常从根到某结点计算，高度通常从某结点到最深叶结点计算。不同教材可能从0或1开始计数，答题前必须确认定义。

## 20. 邻接矩阵与邻接表

邻接矩阵空间为 O(V²)，判断两点是否相邻快，适合稠密图；邻接表空间约为 O(V+E)，适合稀疏图。

## 21. DFS与BFS

DFS 通常借助递归或栈向深处探索；BFS 借助队列按层扩展。在无权图中，BFS 可得到边数最少的路径，DFS一般不能保证。

## 22. Prim与Kruskal

Prim 从一个顶点逐步扩大生成树，常适合稠密图；Kruskal 按边权从小到大选边并用并查集防环，常适合稀疏图。两者求的都是最小生成树。

## 23. 最小生成树与最短路径树

最小生成树使整棵树的边权总和最小；最短路径树使源点到各顶点的路径最短。二者优化目标不同，结果通常不同。

## 24. Dijkstra与Floyd

Dijkstra 解决单源最短路径，不能直接处理负权边；Floyd 解决所有顶点对最短路径，可以处理负权边，但不能存在负权回路。

## 25. 拓扑排序与关键路径

拓扑排序用于有向无环图，给出满足先后约束的顶点序列；关键路径用于 AOE 网，寻找决定工程最短工期的最长路径。

## 26. 折半查找与二叉排序树查找

折半查找要求有序且支持随机访问，适合顺序表；BST 通过指针结构动态维护有序关系，性能取决于树高，退化时可达 O(n)。

## 27. 散列冲突与关键字重复

散列冲突是不同关键字得到同一地址，不代表关键字相同。开放定址法和链地址法都用于处理冲突。

## 28. 装填因子与散列表容量

装填因子是已存元素数与表长的比值。装填因子越高通常越容易发生冲突，但扩大表会增加空间成本。

## 29. 稳定排序与原地排序

稳定性描述相等关键字排序后相对次序是否保持；原地性描述是否只使用常数级额外空间。两者是不同维度，例如归并排序稳定但通常不是原地排序。

## 30. 快速排序与归并排序

快速排序平均 O(n log n)、通常原地但不稳定，最坏 O(n²)；归并排序稳定且最坏 O(n log n)，但数组实现通常需要 O(n) 辅助空间。

## 31. 冒泡排序与简单选择排序

冒泡排序通过相邻交换，通常稳定；简单选择排序每轮选择最值并交换，通常不稳定。二者平均时间复杂度均为 O(n²)。

## 32. 堆排序中的大根堆与最终顺序

升序排序通常先建立大根堆，每次把最大元素交换到当前序列末尾；大根堆本身并不是完整的降序序列。

## 33. 希尔排序与直接插入排序

希尔排序是分组的插入排序，通过逐渐缩小增量改善元素移动；它通常不稳定，复杂度取决于增量序列，不能简单固定为 O(n log n)。

## 34. 基数排序与比较排序

基数排序按关键字各位分配和收集，不依赖元素两两比较；其复杂度与位数和基数有关，因此不受比较排序 O(n log n) 下界的直接限制。

## 35. 分治与动态规划

分治把问题拆成相互独立的子问题后合并；动态规划适用于子问题重叠且具有最优子结构的情况，通过保存结果避免重复计算。
